local:
  components:
    default:
      style: vagrant
    other:
      style: docker-compose
    image:
      style: docker
connect:
  default:
    style: vagrant
  other:
    style: docker-compose
    container: local2
  image:
    style: docker
    container: local2
